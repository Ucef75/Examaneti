import 'package:flutter/material.dart';

class GradeExamsPage extends StatelessWidget {
  const GradeExamsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Grade Exams')),
      body: const Center(child: Text('Grade Exams Page')),
    );
  }
}